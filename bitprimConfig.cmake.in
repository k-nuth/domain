# - Config file for the BITPRIM package
#
#  BITPRIM_INCLUDE_DIRS - include directories for BITPRIM
#  BITPRIM_LIBRARIES    - libraries to link against

get_filename_component(BITPRIM_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
set(BITPRIM_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")

include("${BITPRIM_CMAKE_DIR}/bitprimTargets.cmake")
set(BITPRIM_LIBRARIES bitprim)

find_package(secp256k1 0.0.01 REQUIRED)
