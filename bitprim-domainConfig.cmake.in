# - Config file for the KNUTH_DOMAIN package
#
#  KNUTH_DOMAIN_INCLUDE_DIRS - include directories for KNUTH_DOMAIN
#  KNUTH_DOMAIN_LIBRARIES    - libraries to link against

get_filename_component(KNUTH_DOMAIN_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
set(KNUTH_DOMAIN_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")

include("${KNUTH_DOMAIN_CMAKE_DIR}/kth-domainTargets.cmake")
set(KNUTH_DOMAIN_LIBRARIES kth-domain)

find_package(secp256k1 0 REQUIRED)
