# - Config file for the BITPRIM_DOMAIN package
#
#  BITPRIM_DOMAIN_INCLUDE_DIRS - include directories for BITPRIM_DOMAIN
#  BITPRIM_DOMAIN_LIBRARIES    - libraries to link against

get_filename_component(BITPRIM_DOMAIN_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
set(BITPRIM_DOMAIN_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")

include("${BITPRIM_DOMAIN_CMAKE_DIR}/bitprim-domainTargets.cmake")
set(BITPRIM_DOMAIN_LIBRARIES bitprim-domain)

find_package(secp256k1 0 REQUIRED)
